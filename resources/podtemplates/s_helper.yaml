spec:
  containers:
  - name: jenkins-builder
    resources:
      requests:
        memory: 500Mi
        cpu: 500m
        # ephemeral-storage: 1Gi
      limits:
        memory: 1Gi
        cpu: 1
        # ephemeral-storage: 2Gi
