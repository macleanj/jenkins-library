spec:
  containers:
  - name: jenkins-builder
    resources:
      requests:
        memory: 1Gi
        cpu: 1
        # ephemeral-storage: 1Gi
      limits:
        memory: 3Gi
        cpu: 2
        # ephemeral-storage: 2Gi
